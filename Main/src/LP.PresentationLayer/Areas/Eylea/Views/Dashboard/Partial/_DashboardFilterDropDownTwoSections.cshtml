@model LP.Model.ViewModels.Shared.DropdownViewModel

<select id="@Model.ClientId" class="fl updatePanelGraphs__selectBox box-1-2">
    @foreach (var groupType in Model.DropdownItems)
    {
        <option value="@groupType.Id">
            @groupType.Name
        </option>
    }
</select>
